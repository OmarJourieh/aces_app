import 'package:flutter/material.dart';

class SingleDiseaseScreen extends StatelessWidget {
  const SingleDiseaseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
